import * as fabric from "fabric";

// Region selector logic has been completely removed based on UX feedback.
// "Improve" now acts on the currently selected image directly, or the whole canvas.
export function useRegionSelector() {
  // No-op
}
